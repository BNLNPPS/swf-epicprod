"""swf-epicprod — the production domain of the swf platform.

Distribution package marker and version. Production Django
applications (first: pcs) ship as top-level packages alongside this
one, installed into the swf-monitor runtime.
"""

__version__ = "0.2.0"
